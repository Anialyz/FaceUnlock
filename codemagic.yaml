workflows:
  faceunlock-android: { name: "FaceUnlock Android (legacy toolchain)", max_build_duration: 60, environment: { java: 11 }, scripts: [ { name: "Make gradlew executable", script: "chmod +x ./gradlew" }, { name: "Install legacy Android SDK platform and build-tools", script: "yes | sdkmanager --licenses > /dev/null || true; sdkmanager \"platforms;android-26\" \"build-tools;26.0.2\" > /dev/null || true" }, { name: "Build debug APK", script: "./gradlew assembleDebug --stacktrace --no-daemon" } ], artifacts: [ "app/build/outputs/**/*.apk", "app/build/outputs/**/mapping.txt" ], publishing: { firebase: { firebase_service_account: "$FIREBASE_SERVICE_ACCOUNT_CREDENTIAL_CONTENT", android: { app_id: "1:651577617636:android:73591f8f3c9fcb840690f1", groups: ["testers"] } } } }
